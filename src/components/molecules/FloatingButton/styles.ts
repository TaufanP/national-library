import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    width: 48,
    height: undefined,
    aspectRatio: 1,
    borderRadius: 100,
    bottom: 24,
    right: 24,
    backgroundColor: '#3586f6',
    justifyContent: 'center',
    alignItems: 'center',
  },
  icon: {fontSize: 24, color: '#FFF'},
});

export default styles;
